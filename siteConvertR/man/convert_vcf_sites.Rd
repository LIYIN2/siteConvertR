% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convert_vcf_sites.R
\name{convert_vcf_sites}
\alias{convert_vcf_sites}
\title{Convert VCF sites between old and new genome coordinates}
\usage{
convert_vcf_sites(
  vcf_file,
  output_file = "converted.vcf.gz",
  direction = c("old_to_new", "new_to_old"),
  mapping_file = NULL,
  keep_unmapped = FALSE,
  update_contig_header = TRUE,
  sort_by_position = TRUE
)
}
\arguments{
\item{vcf_file}{Path to input VCF file. It can be \code{.vcf} or \code{.vcf.gz}.}

\item{output_file}{Path to output VCF file. \code{vcfR::write.vcf()} writes gzipped VCF.}

\item{direction}{Conversion direction. Either \code{"old_to_new"} or \code{"new_to_old"}.}

\item{mapping_file}{Optional path to marker correspondence table.
If \code{NULL}, the package internal mapping table will be used.}

\item{keep_unmapped}{Logical. Whether to keep markers not found in the mapping table.
If \code{FALSE}, unmapped markers are removed.}

\item{update_contig_header}{Logical. Whether to update VCF \verb{##contig} header lines.}

\item{sort_by_position}{Logical. Whether to sort variants by chromosome and position.}
}
\value{
Invisibly returns the converted \code{vcfR} object.
}
\description{
This function converts VCF marker IDs, chromosome names, positions,
and optionally VCF contig header lines between old and new genome
coordinate systems.
}
\details{
The mapping table must contain four columns:
\code{old_ID}, \code{old_chr}, \code{new_chr}, and \code{new_ID}.
}
\examples{
\dontrun{
convert_vcf_sites(
  vcf_file = "old.vcf",
  output_file = "new.vcf.gz",
  direction = "old_to_new",
  mapping_file = "mapping.tsv"
)

convert_vcf_sites(
  vcf_file = "new.vcf.gz",
  output_file = "old.vcf.gz",
  direction = "new_to_old",
  mapping_file = "mapping.tsv"
)
}

}
